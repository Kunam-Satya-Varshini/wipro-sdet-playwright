//3. Create a reusable function for currency conversion
function convertCurrency(amount, rate) {
    return amount * rate;
}

// Example (USD to INR approx)
console.log(convertCurrency(10, 83)); // 830